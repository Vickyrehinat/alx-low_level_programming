#include "main.h"

/**
 * more_numbers - print 0 to 14 ten times
 * Return: always 0
 */
void more_numbers(void)
{
	int num, a;

	for (A = 0; a <= 9; a++)
	{
		for (num = 0; num <= 14; num++)
		{i
			if (num > 9)
			{
				_putchar((num / 10) + '0');
			}
			_putchar((num % 10) + '0');
		}
		_putcher('\n');
	}
}
