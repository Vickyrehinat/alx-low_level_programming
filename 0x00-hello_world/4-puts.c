#include <stdio.h>
/**
 * main-serves as the entery point for the program
 *
 * Return: By default returns zero if no errors otherwise return non-zero value.
 */
int main(void)
{
	puts("\"programming is like a building a multilingual puzzle");
	return (0);
}
