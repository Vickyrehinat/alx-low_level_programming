#include "main.h"
/**
 * _puts_recursion - print a string
 * @s: strings
 * Retirn: void
 */
void _puts_recursion(char *s)
{
if (*s)
{
	_putchar(char *s)
		_puts_recursion(s + 1);
}
else
{
	_putchar('\n');
}

}

